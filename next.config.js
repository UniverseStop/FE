/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
};

module.exports = {

    nextConfig,
    //   output:'export'3
    images: {
        domains: ['github.com', 'k.kakaocdn.net']
    },
};

