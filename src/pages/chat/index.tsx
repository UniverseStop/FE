import ChatNav from "@/components/chat/ChatNav";
import React from "react";

const chatList = () => {
    return (
        <div>
            <ChatNav />
            chatList
        </div>
    );
};

export default chatList;
